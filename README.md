![Build status](https://travis-ci.org/ruudud/express-mocha-boilerplate.png)

# Setup

To get dependencies: `npm install` 

# Run

To run tests, issue `make test`, and `make watch` to run test when files
change.
